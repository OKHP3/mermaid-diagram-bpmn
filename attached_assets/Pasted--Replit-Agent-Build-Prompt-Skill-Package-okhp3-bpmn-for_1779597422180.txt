# Replit Agent Build Prompt
## Skill Package: `okhp3-bpmn-for-mermaid`
## Target Repo: OKHP3/mermaid-diagram-bpmn

---

> **How to use this prompt**
> Enter Replit Agent in Plan Mode first. Paste this document as your prompt.
> Review the plan. Then switch to Build Mode to execute.
> Reference links: https://github.com/OKHP3/mermaid-diagram-bpmn · https://okhp3.github.io/mermaid-diagram-bpmn/playground · https://overkillhill.com/projects/bpmn-for-mermaid/

---

## Mission

Build an Agent Skills package (`SKILL.md` format, agentskills.io open standard) inside this existing repository. The skill packages the BPMN DSL generation, validation, and normalization logic of the BPMN for Mermaid application into a headless, agent-consumable format. The goal: any SKILL.md-compatible platform (Claude, Claude Code, GitHub Copilot, Cursor, VS Code, Gemini CLI, OpenAI Codex) can generate valid `bpmn-beta` Mermaid diagrams, validate BPMN structure, and explain the DSL — without opening a browser.

The skill enforces **dual compliance**: all output must satisfy BPMN 2.0.2 Descriptive Conformance subset rules AND Mermaid 11.15 `bpmn-beta` syntax simultaneously. When conflict exists between the two standards, the skill flags it explicitly.

**This is purely additive. Do not touch any existing application code.**

---

## Hard Constraints

- Do NOT modify: `src/`, `public/`, `lib/`, `scripts/` (existing app scripts), `package.json`, `vite.config.*`, `tsconfig.json`, any existing `*.md` outside `skills/`
- Do NOT add: backend, API, telemetry, external dependencies, network calls from scripts
- Do NOT include: BFS references, Builders FirstSource content, employer-owned process examples, employer branding
- Do NOT claim: full BPMN 2.0 execution conformance, BPMN XML compatibility, production Mermaid plugin status
- Do NOT break: `npm run build` — verify it still passes after adding skill files
- The `skills/` folder must NOT be excluded by `.gitignore` — verify this
- All skill scripts must run with `node` only, no install required, no external packages

---

## Phase 0 — Audit First (Plan Mode deliverable)

Before writing any skill files, inspect the existing codebase and produce:

**`docs/skill-readiness-audit.md`**

Contents:
1. Existing parser logic — where it lives, how it works, what it validates
2. Existing DSL vocabulary — where element keywords are defined
3. Existing validation rules — what constraints are currently checked
4. Existing canonical examples — which are clean enough to become skill examples
5. Files that should become skill references (DSL spec, architecture notes, roadmap)
6. Files that must NOT enter the skill (research dumps, proprietary notes, stubs, outdated iterations)
7. Any logic currently UI-coupled that needs extraction before scripting
8. The recommended extraction path for each script module

Do not start Phase 1 until the audit is approved.

---

## Phase 1 — Directory Scaffold

Create this exact folder structure. All folders and files must exist before any content is written.

```
skills/
└── okhp3-bpmn-for-mermaid/
    ├── SKILL.md
    ├── README.md
    ├── references/
    │   ├── bpmn-beta-dsl-reference.md
    │   ├── bpmn-2-element-catalog.md
    │   ├── compliance-matrix.md
    │   ├── pool-lane-message-flow-rules.md
    │   ├── unsupported-and-deferred-features.md
    │   ├── theming-integration.md
    │   └── scope-firewall.md
    ├── assets/
    │   ├── element-vocabulary.json
    │   ├── validation-rules.json
    │   └── canonical-examples/
    │       ├── purchase-order-approval.bpmn-beta.mmd
    │       ├── support-ticket-triage.bpmn-beta.mmd
    │       ├── employee-onboarding.bpmn-beta.mmd
    │       └── cross-pool-collaboration.bpmn-beta.mmd
    ├── scripts/
    │   ├── validate-bpmn-beta.mjs
    │   ├── normalize-bpmn-beta.mjs
    │   ├── lint-process-model.mjs
    │   └── generate-element-inventory.mjs
    └── tests/
        ├── validate-bpmn-beta.test.mjs
        ├── normalize-bpmn-beta.test.mjs
        └── validate-skill.test.mjs
```

---

## Phase 2 — `SKILL.md` (Primary Skill File)

Write `skills/okhp3-bpmn-for-mermaid/SKILL.md` with the following content **verbatim and complete**. No stubs. Every section must have real, actionable content.

---

```markdown
---
name: okhp3-bpmn-for-mermaid
description: Generate, validate, normalize, and explain Mermaid-native bpmn-beta diagrams for BPMN-style process modeling. Use this skill when the user wants to convert process notes into a diagram; when they ask for BPMN, a process flow, a swimlane diagram, a workflow diagram, or bpmn-beta syntax; when they want to validate or repair existing bpmn-beta code; when they mention pools, lanes, gateways, tasks, events, or BPMN 2.0; when they want a Mermaid-native process model they can commit to a repo. Also use when the user references the OverKill Hill P³ BPMN DSL.
license: MIT
metadata:
  version: 0.1.0
  author: OverKill Hill P³
  homepage: https://okhp3.github.io/mermaid-diagram-bpmn/playground
  repository: https://github.com/OKHP3/mermaid-diagram-bpmn
  category: process-diagramming
  tags: bpmn, mermaid, bpmn-beta, business-process, process-modeling, dsl, workflow, lanes, pools, gateways, okhp3
  triggers:
    - create a BPMN diagram
    - draw a business process
    - convert process to mermaid
    - bpmn-beta
    - model this workflow
    - swimlane diagram
    - process flow with gateways
    - BPMN 2.0
    - validate BPMN
    - pools and lanes
    - process modeling
    - mermaid native BPMN
---

## Purpose

This skill generates and validates `bpmn-beta` Mermaid diagrams — the OverKill Hill P³ BPMN-native diagram type for Mermaid. It maps BPMN 2.0 Descriptive Conformance subset elements to a clean, text-first syntax that process analysts can read and LLMs can produce without syntax repair loops.

The output is valid, copy-paste-ready bpmn-beta syntax in a Mermaid code fence — not a flowchart approximation, not BPMN XML, not a description.

## Dual Compliance Requirement

Every output must satisfy two standards simultaneously:

**BPMN 2.0.2 Descriptive Conformance subset** — structural rules, element types, flow routing rules, pool/lane containment rules.

**Mermaid 11.15 bpmn-beta syntax** — keyword vocabulary, ID rules, label quoting, nesting syntax, flow operators.

When these standards conflict, flag the conflict explicitly and explain the limitation. Never silently violate either standard.

---

## When to use this skill

Use when the user asks for BPMN, process flows, bpmn-beta, swimlane diagrams, or process-to-diagram conversion.

## When NOT to use this skill

- Do not claim full BPMN 2.0 execution conformance or XML compatibility.
- Do not use for non-BPMN Mermaid diagrams. Use `okhp3-mermaid-theme-builder` for theming.
- Do not generate BPMN elements that are outside the Descriptive Conformance subset v1 scope.

---

## DSL Reference

### Diagram declaration

Every bpmn-beta diagram begins with:

```
bpmn-beta
```

This must be the first line, or the second line if a `%%{init}%%` theme block is present.

Optional accessibility metadata immediately after the declaration:

```
accTitle: [Diagram title — used by screen readers and Mermaid Chart export tools]
accDescr: [Extended description — optional]
```

### Event keywords

| Keyword | BPMN 2.0.2 Element | Shape | Usage |
|---|---|---|---|
| `start` | Start event (none trigger) | Thin circle | One per primary process path |
| `end` | End event (none) | Thick circle | One or more terminations |
| `event:message` | Message intermediate event | Circle + envelope | Message received or sent |
| `event:timer` | Timer intermediate event | Circle + clock | Time-triggered step |
| `event:error` | Error end event | Thick circle + lightning bolt | Error termination path |

Syntax:

```
start         [id]  "[Label]"
end           [id]  "[Label]"
event:message [id]  "[Label]"
event:timer   [id]  "[Label]"
event:error   [id]  "[Label]"
```

### Task keywords

| Keyword | BPMN 2.0.2 Element | BPMN Marker | Usage |
|---|---|---|---|
| `task` | Abstract task | Rounded rectangle (no marker) | Generic work step, type unspecified |
| `task:user` | User task | Person icon | Human-performed work |
| `task:service` | Service task | Gear icon | Automated or system-executed step |
| `task:script` | Script task | Scroll icon | Script execution step |

Syntax:

```
task         [id]  "[Label]"
task:user    [id]  "[Label]"
task:service [id]  "[Label]"
task:script  [id]  "[Label]"
```

### Gateway keywords

| Keyword | BPMN 2.0.2 Element | Shape | Routing logic |
|---|---|---|---|
| `xor` | Exclusive gateway (XOR) | Diamond + X | Exactly one outgoing path activates |
| `and` | Parallel gateway | Diamond + + | All outgoing paths activate simultaneously |
| `or` | Inclusive gateway | Diamond + O | One or more outgoing paths activate |

Syntax:

```
xor [id] "[Label]"
and [id] "[Label]"
or  [id] "[Label]"
```

### Sequence flow

All sequence flows use `-->`. Conditional labels follow a colon:

```
[source-id] --> [target-id]
[source-id] --> [target-id]: "[Condition label]"
```

Gateway outgoing flows must have condition labels unless routing is unconditional (join flows do not need labels).

Default flows (from XOR gateways) may use `==>`:

```
[gateway-id] ==> [default-target-id]
```

### Pool and lane structure

```
pool [pool-id] "[Pool Label]" {

  lane [lane-id] "[Lane Label]" {
    [elements declared here]
  }

  lane [lane-id-2] "[Lane Label 2]" {
    [elements declared here]
  }

  [cross-lane flows declared here — preferred location for inter-lane flows]
}
```

Elements must be declared inside a lane block if the diagram uses lanes. Cross-lane flows may be declared either inside the pool block body (preferred) or inside the source lane block.

Nested lanes (lanes within lanes) are supported by the BPMN 2.0.2 spec but have limited rendering reliability in Mermaid 11.15. Use with caution and verify in the playground.

### Message flow (between pools)

```
pool [pool-a] "[Pool A Label]" { ... }
pool [pool-b] "[Pool B Label]" { ... }

message [source-id] --> [target-id]: "[Message label]"
```

Message flow is experimental in the current prototype renderer. Flag this limitation when generating cross-pool diagrams.

---

## Generation Workflow

Execute in order. Do not skip steps.

### Step 1 — Extract process information

From the user's input (natural language, existing flowchart, table, prose, bullet list):

- **Actors/participants** → these become lanes (single org) or pools (multiple orgs)
- **Process trigger** → start event (what initiates the process, and what type)
- **End states** → end events (success path, error path, rejection path — each gets an end event)
- **Work steps** → tasks (classify: user task for human, service task for automated, script task for scripted, abstract task when unspecified)
- **Decision points** → gateways (classify: XOR for exclusive choice, AND for parallel, OR for inclusive)
- **Decision outcomes** → gateway outgoing flows with condition labels
- **Waiting points or triggers** → intermediate events (timer, message)
- **Inter-organizational messages** → message flows between pools

If ambiguous, state your assumption explicitly before producing the diagram. Do not stall with clarifying questions — interpret and label.

### Step 2 — Choose structural model

**Single pool, multiple lanes:** All actors are inside one organizational boundary. One company, multiple departments or roles. Default for most business process documentation.

**Multiple pools with message flows:** Two or more organizations with interactions across boundaries. Customer-to-vendor, company-to-partner, internal system-to-external system. Use when the process crosses an organizational or system ownership boundary.

**Flat (no pool/lane):** Simple linear processes with one actor, or when the user explicitly requests a flat model. Use sparingly.

### Step 3 — Assign IDs

Assign short, consistent IDs before writing the DSL:

- Start events: `s1`, `s2` (or semantic: `start-request`)
- End events: `e1`, `e2` (or semantic: `end-approved`)
- Tasks: `t1`, `t2` (or semantic: `review-request`)
- Gateways: `g1`, `g2` (or semantic: `approved-gate`)
- Intermediate events: `ev1`, `ev2`

IDs must be unique within the diagram. Use lowercase, no spaces, no special characters except hyphens and underscores. Do not use reserved keywords as IDs: `pool`, `lane`, `start`, `end`, `task`, `xor`, `and`, `or`, `message`, `event`.

### Step 4 — Write the DSL

Apply all DSL Reference rules above plus:

**Structural constraints (BPMN 2.0.2 — always enforce):**
- Every diagram must have at least one `start` event and at least one `end` event
- Sequence flows must not cross pool boundaries — use `message` flows for cross-pool
- XOR gateway: every outgoing flow must have a condition label; at least two outgoing flows
- AND gateway (split): must have a corresponding AND gateway (join) that reunites the paths, unless each parallel path terminates in a separate end event
- Every element must appear in at least one flow declaration — no orphans
- End events must not have outgoing sequence flows
- Start events must not have incoming sequence flows

**Syntax constraints (Mermaid 11.15 — always enforce):**
- All labels must be in double quotes
- IDs must be valid — no spaces, no reserved keywords
- Pool and lane brackets must be properly opened and closed
- `bpmn-beta` must appear as the diagram type keyword

### Step 5 — Self-validate

Before outputting, check every item:

- [ ] `bpmn-beta` on line 1 (or line 2 if `%%{init}%%` is present)
- [ ] At least one `start` event exists
- [ ] At least one `end` event exists
- [ ] All IDs referenced in flows are declared as elements
- [ ] All `xor` gateways have at least two outgoing labeled flows
- [ ] No orphan elements (every element appears in at least one flow)
- [ ] Pool and lane brackets are properly nested and closed
- [ ] All string labels are in double quotes
- [ ] No reserved keywords used as IDs
- [ ] Sequence flows do not cross pool boundaries
- [ ] Cross-pool flows use `message` syntax

If any check fails, fix before outputting.

### Step 6 — Output

Produce the complete bpmn-beta diagram in a fenced Mermaid code block:

```mermaid
bpmn-beta
accTitle: [Descriptive title]

pool [id] "[Pool Label]" {

  lane [id] "[Lane Label]" {
    [element declarations]
  }

  [cross-lane flows]
}
```

Immediately after the code block, provide a compact element inventory:

**Elements:** [n] tasks ([n] user, [n] service, [n] script, [n] abstract), [n] events ([n] start, [n] end, [n] intermediate), [n] gateways, [n] lanes, [n] pools
**Flows:** [n] sequence flows[, [n] message flows if applicable]
**Assumptions:** [List any interpretive decisions made in Steps 1–3]

---

## Validation Workflow

When the user provides existing bpmn-beta code for validation:

1. Parse the structural model: identify all declared elements, their types, and all flows
2. Check every element declaration for correct keyword spelling
3. Check every flow for declared source and target IDs
4. Check XOR gateway cardinality (minimum 2 outgoing labeled flows)
5. Check AND gateway split/join pairing
6. Check bracket nesting (pool ⊃ lane ⊃ elements, all properly closed)
7. Check all labels are double-quoted
8. Check no reserved keywords used as IDs
9. Check sequence flows do not cross pool boundaries
10. Check all elements appear in at least one flow

Report format:
- First line: `BPMN Compliance: PASS` or `BPMN Compliance: FAIL ([n] issues found)`
- Numbered list of issues: `[Issue N] Line ~[N]: [Description] — [Correction]`
- Corrected diagram as final output if issues were found

---

## Normalization Workflow

When the user asks to normalize or clean existing bpmn-beta code:

1. Standardize ID naming to lowercase kebab format
2. Ensure all labels are double-quoted
3. Move cross-lane flows to pool body level (outside lane blocks)
4. Ensure `accTitle` is present; generate a descriptive one if missing
5. Sort element declarations within each lane: start events first, then tasks in flow order, then gateways, then end events
6. Validate after normalization
7. Output normalized code and a summary of changes made

---

## Explanation Workflow

When the user asks about a specific element, construct, or DSL concept:

1. State the element name and its BPMN 2.0.2 equivalent
2. Show the exact bpmn-beta syntax in a code block
3. Give one usage rule
4. Show a minimal working example (3–5 elements, 2–3 flows)

Do not reproduce the full DSL reference in an explanation response. Answer the specific question.

---

## Scope Boundaries

**In scope — generate these:**
- start, end events (none trigger)
- event:message, event:timer (intermediate)
- event:error (end event)
- task, task:user, task:service, task:script
- xor, and, or gateways
- Sequence flows (standard, conditional, default)
- Message flows between pools
- Pools and lanes
- accTitle and accDescr

**Out of scope — do not generate:**
- Boundary events on tasks
- Event sub-processes
- Transaction sub-processes
- Compensation events
- Data objects and data stores
- Choreography diagrams
- Conversation diagrams
- BPMN XML import or export
- Process engine execution semantics
- Multi-instance markers
- Call activities

When the user requests an out-of-scope element, state clearly that it is outside the current DSL scope and offer the closest available approximation with an explicit note about the difference.

---

## Theming Integration

bpmn-beta diagrams honor Mermaid themeVariables. If the user also has the `okhp3-mermaid-theme-builder` skill loaded and requests theming, prepend the `%%{init}%%` block:

```mermaid
%%{init: { 'theme': 'base', 'themeVariables': { ... } }}%%
bpmn-beta
accTitle: [title]
...
```

If no theme is requested, output the diagram without any init block. Do not default to a specific palette.

At the end of diagram generation, if both skills are loaded, offer: "Would you like to apply a color theme to this diagram? I have the okhp3-mermaid-theme-builder skill loaded."

---

## Output Rules

- Always produce the complete, syntactically valid bpmn-beta diagram — never a partial sketch
- Never produce bpmn-beta from memory of BPMN XML syntax or bpmn-js notation
- When uncertain about element classification, use `task` or `xor` as the conservative default and state the assumption
- Do not produce Mermaid flowchart syntax as a substitute for bpmn-beta
- The output is the product. Keep explanation text minimal unless the user asks for explanation.
- Never claim the output is executable BPMN or BPMN 2.0 XML-compatible

---

## References

Load on demand when the body instructions reference them:
- `references/bpmn-beta-dsl-reference.md` — formal grammar spec, BNF notation, complete keyword table
- `references/bpmn-2-element-catalog.md` — full BPMN 2.0.2 element catalog for compliance decisions
- `references/compliance-matrix.md` — BPMN element vs. Mermaid 11.15 support status matrix
- `references/pool-lane-message-flow-rules.md` — detailed containment and routing rules
- `references/unsupported-and-deferred-features.md` — what is out of scope for v1 and why
- `references/theming-integration.md` — how to compose with okhp3-mermaid-theme-builder
- `references/scope-firewall.md` — what must never appear in skill output

## Scripts

Load when deterministic processing is needed:
- `scripts/validate-bpmn-beta.mjs` — structural + syntax validation
- `scripts/normalize-bpmn-beta.mjs` — canonical formatting
- `scripts/lint-process-model.mjs` — process modeling quality checks
- `scripts/generate-element-inventory.mjs` — element count and type summary
```

---

## Phase 3 — Reference Files

Write full content for each. No stubs.

### `references/bpmn-beta-dsl-reference.md`

The formal grammar specification for bpmn-beta. Must include:
- BNF-style notation for every element type, pool, lane, flow, and message flow
- Complete keyword table (all elements, their syntax, and parameter types)
- ID naming rules and reserved word list
- Label quoting rules
- Nesting rules: what can contain what
- Flow declaration placement rules: which flows go inside lanes vs. pool body
- Full annotated example diagram covering all supported elements

### `references/bpmn-2-element-catalog.md`

BPMN 2.0.2 element reference grouped by category (Flow Objects, Connecting Objects, Swimlanes, Artifacts). For each element:
- Canonical name
- Element type and category
- Required and optional attributes
- Allowed incoming and outgoing connections
- One-line description
- Shape description (what it looks like)

Scope: Descriptive Conformance subset only. Do not include Analytic or Common Executable conformance elements.

### `references/compliance-matrix.md`

Three-column table: `BPMN 2.0.2 Element | Mermaid 11.15 bpmn-beta Support | Notes`

Support status values: `Full`, `Partial`, `Not Supported`

Cover all elements in the Descriptive Conformance subset. For "Partial" and "Not Supported" entries, explain the gap and the nearest available approximation in bpmn-beta.

### `references/pool-lane-message-flow-rules.md`

Detailed rules for:
- Pool containment: what is inside a pool vs. outside
- Lane containment: what is inside a lane, what can span lanes
- Cross-lane flow placement (pool body vs. lane body)
- Message flow syntax for cross-pool communication
- Nested lanes — supported spec vs. rendering limitations in Mermaid 11.15
- Common errors and their corrections

Include at least four fully-worked structural examples showing correct vs. incorrect placement.

### `references/unsupported-and-deferred-features.md`

Honest scope document. Must include:
- What bpmn-beta IS: descriptive text-first BPMN, AI-generatable, Mermaid-native
- What bpmn-beta IS NOT: executable, XML-compatible, full BPMN conformance, production Mermaid plugin yet
- Complete list of out-of-scope v1 elements with a one-sentence rationale for each deferral
- Current prototype parser limitations vs. planned Langium grammar
- Rendering limitations specific to pool/lane and message flows in the current prototype renderer
- How to report issues or contribute to scope expansion

This is a trust document. Ship it verbatim.

### `references/theming-integration.md`

Instructions for composing with `okhp3-mermaid-theme-builder`:
- When to add theming to a bpmn-beta diagram
- Exact syntax for prepending `%%{init}%%` to bpmn-beta
- Which themeVariables are most impactful for BPMN diagrams
- How to invoke the Mermaid Theme Builder skill from BPMN skill context
- A worked example: unthemed bpmn-beta → themed bpmn-beta with ocean-depth palette

### `references/scope-firewall.md`

List what must never appear in skill output:
- BFS (Builders FirstSource) process examples, terminology, or branding
- Employer-owned business processes or workflow examples
- Full BPMN 2.0 conformance claims
- BPMN XML syntax or element names
- bpmn-js syntax or API references
- Elements outside the Descriptive Conformance subset presented as supported
- Hallucinated bpmn-beta keywords not in the DSL Reference

---

## Phase 4 — Assets

### `assets/element-vocabulary.json`

JSON array of all bpmn-beta elements. Each object:

```json
{
  "keyword": "task:user",
  "bpmn-equivalent": "User Task",
  "bpmn-category": "Activity",
  "bpmn-element-type": "Task",
  "shape": "Rounded rectangle with person icon",
  "scope": "v1",
  "experimental": false,
  "syntax": "task:user [id] \"[Label]\"",
  "notes": "Human-performed work. Use when a person is responsible for the step."
}
```

Include all supported elements: all event keywords, all task keywords, all gateway keywords. Mark experimental elements with `"experimental": true`.

### `assets/validation-rules.json`

JSON array of validation rules. Each rule object:

```json
{
  "rule-id": "VR-001",
  "category": "structure",
  "description": "Every diagram must have at least one start event",
  "standard": "BPMN-2.0.2",
  "type": "existence",
  "severity": "error",
  "check": "count(start) >= 1",
  "message": "No start event found. Add a 'start [id] \"[Label]\"' declaration."
}
```

Include at minimum these rule IDs:
- VR-001: At least one start event
- VR-002: At least one end event
- VR-003: All flow source IDs are declared elements
- VR-004: All flow target IDs are declared elements
- VR-005: XOR gateways have at least two outgoing flows
- VR-006: XOR gateway outgoing flows have condition labels
- VR-007: No orphan elements
- VR-008: Sequence flows do not cross pool boundaries
- VR-009: Pool and lane brackets are balanced
- VR-010: All labels are double-quoted
- VR-011: No reserved keywords used as IDs
- VR-012: AND gateway splits have corresponding AND joins (or deliberate parallel end events)

### `assets/canonical-examples/*.bpmn-beta.mmd`

Four fully valid bpmn-beta diagrams used as skill reference examples and test fixtures. Each must pass all validation rules.

**`purchase-order-approval.bpmn-beta.mmd`**
Three-lane single-pool process: Requester, Manager, Procurement. One XOR gateway (Approved?), one rejection path, one PO issuance path. Uses task:user, task:service, start, end, xor.

**`support-ticket-triage.bpmn-beta.mmd`**
Two-lane process: Customer, Support Team. One event:message (ticket received), one XOR for priority, one AND gateway for parallel notification. Uses task:user, task:service, event:message, event:timer, xor, and.

**`employee-onboarding.bpmn-beta.mmd`**
Three-lane single-pool process: HR, IT, Hiring Manager. Sequential handoffs, parallel setup tasks (AND gateway), multiple end events. Demonstrates cross-lane flow pattern.

**`cross-pool-collaboration.bpmn-beta.mmd`**
Two-pool process: Company, Vendor. Message flows between pools. One XOR gateway per pool. Demonstrates message flow syntax and cross-pool isolation of sequence flows.

---

## Phase 5 — Scripts

All scripts: ES module format (`.mjs`), no external dependencies, runs with `node scripts/filename.mjs`.

### `scripts/validate-bpmn-beta.mjs`

Exports:

```javascript
validateBpmnBeta(bpmnBetaCode: string): {
  valid: boolean,
  errors: Array<{ ruleId: string, message: string, element?: string }>,
  warnings: Array<{ message: string, element?: string }>
}
```

Implementation must:
1. Strip the `%%{init}%%` block if present before parsing
2. Confirm `bpmn-beta` is the diagram type keyword
3. Parse all element declarations into a registry: `{ id: string, keyword: string, type: string }`
4. Parse all flow declarations: `{ from: string, to: string, label?: string, type: 'sequence' | 'message' | 'default' }`
5. Parse pool/lane structure
6. Run all rules from `assets/validation-rules.json` against the parsed model
7. Return structured result

### `scripts/normalize-bpmn-beta.mjs`

Exports:

```javascript
normalizeBpmnBeta(bpmnBetaCode: string): {
  normalized: string,
  changes: string[]
}
```

Implementation must:
1. Ensure `bpmn-beta` keyword on line 1 (or 2 after init block)
2. Ensure `accTitle` is present; generate "Business Process Diagram" if missing
3. Normalize all IDs to lowercase-kebab
4. Ensure all labels are double-quoted
5. Move cross-lane flows to pool body level
6. Run validate-bpmn-beta on result and confirm it passes
7. Return normalized code and list of changes applied

### `scripts/lint-process-model.mjs`

Exports:

```javascript
lintProcessModel(bpmnBetaCode: string): {
  score: number,
  issues: Array<{ type: 'quality' | 'naming' | 'accessibility', message: string, suggestion: string }>
}
```

Linting checks (quality, not structural errors):
- All gateways have meaningful labels (not just "Gateway" or "G1")
- All tasks have descriptive labels (not just "Task" or "T1")
- accTitle is present and meaningful (not generic placeholder)
- Intermediate events are used where process timing is relevant
- Parallel gateways have matching joins
- No more than 15 elements in a single lane (readability threshold)

### `scripts/generate-element-inventory.mjs`

Exports:

```javascript
generateElementInventory(bpmnBetaCode: string): {
  pools: number,
  lanes: number,
  tasks: { total: number, user: number, service: number, script: number, abstract: number },
  events: { total: number, start: number, end: number, intermediate: number },
  gateways: { total: number, xor: number, and: number, or: number },
  flows: { sequence: number, message: number, default: number }
}
```

---

## Phase 6 — Tests

All tests: ES module format, pure Node.js test runner (`node --test`), no Jest/Vitest required.

### `tests/validate-bpmn-beta.test.mjs`

Test cases:
- All four canonical examples → PASS
- Missing start event → error VR-001
- Missing end event → error VR-002
- Flow referencing undeclared ID → error VR-003 or VR-004
- XOR with only one outgoing flow → error VR-005
- XOR outgoing flows without labels → error VR-006
- Orphan element → error VR-007
- Sequence flow crossing pool boundary → error VR-008
- Unbalanced brackets → error VR-009
- Unquoted label → error VR-010
- Reserved keyword as ID → error VR-011
- Out-of-scope element keyword (boundary event, compensation) → warning or error

### `tests/normalize-bpmn-beta.test.mjs`

Test cases:
- Valid diagram → no changes, same structure
- Missing accTitle → accTitle added, change logged
- ID with spaces → normalized to kebab, change logged
- Cross-lane flow inside lane block → moved to pool body, change logged
- Unquoted label → quoted, change logged
- Output of normalization passes validate-bpmn-beta

### `tests/validate-skill.test.mjs`

Skill metadata tests:
- SKILL.md YAML frontmatter parses without errors
- `name` field exactly equals `okhp3-bpmn-for-mermaid`
- `description` field length is between 100 and 1024 characters
- All `references/` files listed in SKILL.md body exist on disk
- All `scripts/` files listed in SKILL.md body exist on disk
- `assets/element-vocabulary.json` parses as valid JSON
- `assets/validation-rules.json` parses as valid JSON
- All four canonical example files exist and contain `bpmn-beta` as the diagram type keyword
- All four canonical examples pass validate-bpmn-beta

---

## Phase 7 — README and Integration

### `skills/okhp3-bpmn-for-mermaid/README.md`

Contents:
- What the skill does (2 sentences)
- How to install in Claude Code, GitHub Copilot, Cursor, VS Code, Gemini CLI (one block per platform)
- How to trigger the skill (3–5 example prompts)
- Example output: one natural-language process description → complete bpmn-beta diagram
- Cross-skill integration: how to combine with okhp3-mermaid-theme-builder
- Link back to the live playground and GitHub repo

### Root `README.md` update

Add `## Agent Skill` section after existing usage docs:

```markdown
## Agent Skill

This repository includes a SKILL.md-compatible agent skill at `skills/okhp3-bpmn-for-mermaid/`.

The skill delivers bpmn-beta DSL generation, validation, and normalization to any SKILL.md-compatible
agent (Claude, GitHub Copilot, Cursor, VS Code, Gemini CLI, OpenAI Codex) without requiring the browser playground.

**Install:** Copy the `skills/` folder into your project or agent workspace.
**Configure:** Point your agent to discover skills from the local path per [agentskills.io](https://agentskills.io).
**Trigger example:** "Convert these process notes into a bpmn-beta diagram with pools and lanes, then validate it."
```

---

## Acceptance Criteria

| # | Criterion | Pass Condition |
|---|---|---|
| 1 | SKILL.md exists | `skills/okhp3-bpmn-for-mermaid/SKILL.md` present and non-empty |
| 2 | YAML frontmatter valid | Parses without error; all required keys present |
| 3 | Folder name matches | `name` in frontmatter exactly equals `okhp3-bpmn-for-mermaid` |
| 4 | Description complete | ≥ 100 chars; covers generation, validation, normalization trigger scenarios |
| 5 | All reference files exist | All 7 files in `references/` present and non-stub |
| 6 | Asset JSON valid | element-vocabulary.json and validation-rules.json parse without error |
| 7 | All canonical examples present | All four `.bpmn-beta.mmd` files exist |
| 8 | Canonical examples valid | All four pass validate-bpmn-beta with zero errors |
| 9 | Scripts run | `node scripts/validate-bpmn-beta.mjs` completes without error |
| 10 | Tests pass | `node --test tests/` shows all tests passing |
| 11 | Four invalid inputs fail correctly | Known-bad diagrams produce descriptive errors, not silent pass |
| 12 | No BPMN XML in outputs | No `<bpmn:`, `<process`, or `xmlns` in any skill file |
| 13 | No existing app scripts touched | `scripts/` at repo root unchanged |
| 14 | No BFS content | `grep -ri "builders firstsource\|bfs\." skills/` returns nothing |
| 15 | Build unaffected | `npm run build` completes successfully |
| 16 | Not in .gitignore | `git check-ignore skills/` returns nothing |
| 17 | README updated | Root README contains `## Agent Skill` section |
| 18 | Dual compliance stated | SKILL.md body contains the dual compliance requirement |
| 19 | Scope boundaries present | Out-of-scope element list is complete and explicit |
| 20 | No full BPMN conformance claims | No text claims "full BPMN 2.0 conformance" or "executable BPMN" |

---

## Git Commit

After all acceptance criteria pass:

```
git add skills/
git add README.md
git add docs/skill-readiness-audit.md
git commit -m "feat(skills): add okhp3-bpmn-for-mermaid SKILL.md agent skill v0.1.0"
git push origin main
```

---

*OverKill Hill P³ · overkillhill.com · May 2026*