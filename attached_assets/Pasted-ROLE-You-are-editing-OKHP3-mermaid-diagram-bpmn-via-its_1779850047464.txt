ROLE
You are editing OKHP3/mermaid-diagram-bpmn via its Replit project.
This app diverges from the OverKill Hill P³ Forge motif on 9 of 12 visual axes.
The canonical spec is the sibling app's src/styles/forge-tokens.css and the
overkillhill.com theme.css :root forge scope. Bring this app to conformance.
Target: artifacts/mermaid-diagram-bpmn/src/index.css and index.html. Apply the
same fixes to artifacts/mockup-sandbox/.

PREFERRED PATH
Copy src/styles/forge-tokens.css from OKHP3/mermaid-theme-builder into this repo
at the same path, import it first in index.css, and delete this app's hand-rolled
token block. If you cannot reach the sibling repo, apply the explicit fixes below
to reach identical values.

EVIDENCE OF DIVERGENCE (current BPMN value -> required canonical value)
1. index.html font load: Fraunces + Inter -> Alfa Slab One + DM Sans.
   Replace the Google Fonts <link> with:
   family=Alfa+Slab+One&family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500
2. --app-font-sans: 'Inter' -> 'DM Sans', system-ui, sans-serif
3. --app-font-display: 'Fraunces', Georgia, serif -> 'Alfa Slab One', system-ui, sans-serif
4. --background light: 40 24% 92% (#f0ece4) -> 33 18% 94% (#f0ebe5)
5. --foreground light: 28 45% 8% (#1d1108 espresso) -> 222 47% 11% (#0f172a navy)
6. --card light: 42 50% 97% (#faf7f1) -> 34 35% 95% (#f6f2ee)
7. Dark mode family: olive hue 88 -> slate-blue. Copy the sibling dark block
   wholesale: --background 224 30% 8%, --foreground 220 14% 92%,
   --card 224 25% 11%, --border 224 20% 18%, --primary 25 63% 58%. Do not recompute.
8. --okh-header-bg: #1c2118 -> #1c3a34 (teal). Header text #e6dfc9 -> #e5e7eb.
   Header border -> rgba(255,255,255,0.07).
9. --radius: 0.5rem -> 0.75rem
10. Mermaid output in src/lib/bpmn-styles.ts: MERMAID_FALLBACK_THEME generic
    values (#1890ff, #333, #999) -> canonical OKH P³ set: primaryColor #111827,
    lineColor #c46a2c, nodeBorder #c46a2c, clusterBkg #0d1117, textColor #e5e7eb.
    Prefer importing OKH_P3_MERMAID_THEME from the sibling rather than re-typing.
11. Default mode: ensure light when no preference is stored; no .dark on root at
    load; do not follow prefers-color-scheme by default.

DO NOT TOUCH
Layout, spacing scale, component structure, pill button shapes (rounded-full is
fine). Primary accent is already correct (#c46a2c); leave it.

CONSTRAINTS
No em dashes anywhere. No new fonts/hues/radii beyond the canonical set above.
After changes, render one BPMN diagram in light and dark and confirm: DM Sans
body, Alfa Slab One headings, teal header, rust-orange flows, navy ink (light),
slate dark mode. Grep src for stragglers: Fraunces, Inter, font-serif, #1890ff,
0.5rem, hue 88. Output a pass/fail table against items 1-11 plus a one-paragraph
diff summary.
